音声合成用の日本語アクセントデータを可視化し、実際の音声と聞き比べながらアクセント句境界およびアクセント位置を修正するツール。

---

- パッケージインストールはpnpmを使う。CDNは使わない
- Vue + Vite を使う
- ESLintとPrettierを導入する
- TypeScript Strictモードを使う
- VitestとPlaywrightを使う
- tailwindCSSを使う
- Pinia / Vuex は使わない
- アイコンライブラリはheroiconsを使う
- 文字列をTypeScriptのobjectに読み込む際はzodでバリデーションする
- UIやコメントやエラーメッセージは基本的に日本語で書く

- 現在の進捗を把握してから、小さく実装できるものを見極め、区切って実装する
- 実装前に周囲のコードをよく読んで完全に理解し、最も良い実装方法を確信してから取り掛かる
- 設計を後から足していきやすい形に実装する
- 実装完了後は `pnpm typecheck`、`pnpm format`、`pnpm lint` を実行する。dev 起動や build チェックは不要

- 冗長性を持たせることは禁止
  GOOD: シグネチャを破壊的に変更する
  BAD: 既存コードも新コードも両方動くようにする
- すべてのエラー・例外の握りつぶしは禁止
- フォールバック処理を書かない。エラーが起きたら止まるのが正解
- ちゃんとしたエラーハンドリングはOK
- ハンドリングしないエラーは最終的にすべて上に伝播させ、エラーバウンダリーで拾う。そこでconsole.errorするだけ
- 正当な事情があるフォールバック処理には、事情がわかるコメントかconsole.logを入れる。将来ログが嘘にならないよう注意する。断言できないなら「おそらく」とするなど。
- エラーメッセージの文字列でエラーを判別して対処するのは禁止。エラークラスを定義する
- 引数のデフォルト値は禁止
- `!` は禁止
- 論理的にありえないが型システム上必要なif文を書く場合は `UnreachableError` を定義してthrowする
- falsy/truthyチェックは禁止
- `null` や `undefined` との比較に `===` は禁止。`==` を使う。どうしても厳密一致が必要な場合はインラインでignoreする
- `as` はどうしても必要なとき以外は禁止。型が正しいなら `satisfies` を使い、ロジックがおかしいならロジックを修正する
- 複数の変数の状態が連動するはずな場合はUnion等でまとめる
  GOOD:
  ```ts
  {isOpen: false} | {isOpen: true; title: string}
  ```
  BAD:
  ```ts
  isOpen: boolean;
  title: string;
  ```
- TypeScript設定かESLint設定でルールを強制する。TypeScript設定があるならそちらが良い

- サービスはすべてシングルトンインスタンスで提供し、importで直接参照する
- シングルトンじゃないクラスは作らない
- 状態管理はシングルトンクラスで行う
- Undo/Redoはシングルトンクラスで管理する
- グローバルなアクション管理シングルトンサービスを用意し、アクションと処理を紐付ける形にする
- ショートカットキーやメニューバーはアクション管理サービスを通して実装する
- すべてのダイアログは共通シングルトンサービスで管理する
- 状態は全体で1ヶ所で持つ。状態管理クラスを入れ子にするのはOKで、状態が親から渡ってくる設計にする
- スクロール位置など揮発する状態は状態管理クラスではなくコンポーネント内で管理する

- 保存データにはアプリバージョンを書き込む。アプリバージョンが新しければマイグレーションを行う
- マイグレーション失敗時は、設定を初期化するかどうかの確認ダイアログを出す
- 試作段階ではマイグレーション用の関数を用意するまでは良いが、実際のマイグレーション内容を書くことは禁止
- 設定変更のたびに自動保存する
- プロジェクト状態を保存し、再オープン時になるべく同じ画面を再現する

- デザインはシンプルで、シンプルすぎず、ダサさを感じない UI にする
- 設定は奥まった位置ではなく、簡単に変えられる位置に置く
- 絵文字は禁止
- z-indexはどうしても必要な場合以外で使わず、レイアウトの順番かCSSで解決する
- アニメーションはどうしても必要な場合以外で使わない。きびきび動いた方がUXが良い。
- ブラウザ標準ダイアログはテストしづらいため、どうしてもそれしかない場合以外は使わない
- D&Dでファイル追加できるUIでは、ドロップ受け入れ可能な状態になったことがわかるようUIを変化させる
- ダイアログは表示タイミングで動的に追加する。1つのコンポーネントを指定するだけで動く形が理想
- ダイアログは複数同時表示可能にする。後から開いたものが上に重なり、閉じたときはそのダイアログだけ閉じる
- 重なり順はDOMツリーの順番で解決する
- ダイアログのボタンはフッターに置く。右上の✕ボタンはやめる
- フッター右下にはダイアログを終わらせるボタン（閉じる・キャンセルなど）以外は置かない
- ダイアログはシンプルにする。エラーアイコン等は不要
- サービスはインターフェイスを用意し、モックを差し替えられる形にする

- URLパラメータでモックに切り替えられるようにする。個別のモックだけ適用するパラメータと、全モックをまとめて適用できるパラメータも用意する
- まず本物のAPIを使わずモックで動作確認できるようにする
- Blob、URLなど、類似機能が既にある場合は踏襲し、自作しない
