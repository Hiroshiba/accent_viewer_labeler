# プロトタイプ実装計画

## Context

`初期案仕様書.md` に記載された日本語アクセントデータ可視化・修正ツールを、現在のテンプレート状態（Vue 3 + Vite + Electron）から段階的に実装する。モックファースト・基盤から積み上げる方針で、各ステップが独立して動作確認可能な形にする。

## ディレクトリ構成（最終形）

```
/
  pnpm-workspace.yaml
  package.json (workspace root)
  eslint.config.js
  packages/
    shared/       ← Vueアプリ本体（コンポーネント、サービス、型、ドメインロジック）
    web/          ← ブラウザ版エントリ
    electron/     ← Electron版エントリ + メインプロセス + preload
```

---

## チェックリスト

### ステップ 0: pnpm workspace化とモノレポ構成

- [x] `pnpm-workspace.yaml` 作成、`packages/shared`, `packages/web`, `packages/electron` に分割
- [x] 既存 `src/`, `electron/` を各パッケージに移動
- [x] 各パッケージに `package.json`, `tsconfig.json`, `vite.config.ts` 配置
- [x] ルート `package.json` をワークスペースルート用に修正、`eslint.config.js` をモノレポ対応に
- [x] `eslint.config.js` に CLAUDE.md 制約ルール追加（`@typescript-eslint/no-non-null-assertion: "error"` 等）
- [x] **確認**: `pnpm install` 成功、`pnpm --filter web dev` でブラウザ版空ページ表示、`pnpm typecheck` 通過

### ステップ 1: Tailwind CSS, @headlessui/vue, @heroicons/vue, zod, Dexie.js 導入

- [x] 各パッケージに依存追加・Tailwind CSS v4 設定（`@tailwindcss/vite` plugin）
- [x] `style.css` を Tailwind ディレクティブに置き換え
- [x] `index.html` の `lang="ja"` 変更、タイトル変更
- [x] Vitest / Playwright インストール、`vitest.config.ts` / `playwright.config.ts` 配置（テスト本体はプロトタイプ段階では書かない）
- [x] **確認**: Tailwind ユーティリティクラスが効く、`pnpm typecheck` 通過

### ステップ 2: 基盤型定義とエラークラス

- [x] `shared/src/types/project.ts` — ProjectData, SampleData 等の型 + zod スキーマ
- [x] `shared/src/types/accent.ts` — MoraInterval, PitchLevel 等
- [x] `shared/src/types/settings.ts` — DisplayMode, AutoScrollMode, PlaybackSpeed
- [x] `shared/src/errors.ts` — UnreachableError, ParseError 等
- [x] **確認**: `pnpm typecheck` 通過

### ステップ 3: FsAdapter 抽象インターフェースとモック実装

- [x] `shared/src/services/fs-adapter/interface.ts` — FsAdapter インターフェース
- [x] `shared/src/services/fs-adapter/mock.ts` — MockFsAdapter（ハードコードテストデータ）
- [x] `shared/src/services/mock-config.ts` — URLパラメータ `?mock=all` / `?mock=fs` による切り替え
- [x] `shared/src/mock-data/samples.ts` — 3-5件の日本語テストデータ
- [x] **確認**: `?mock=all` でモック選択される、`pnpm typecheck` 通過

### ステップ 4: シングルトンサービス基盤

- [x] `shared/src/services/app-state/` — AppStateService（Vue `shallowRef` 内部使用）
  - 状態: `{ phase: "empty" } | { phase: "loading"; ... } | { phase: "editing"; project: ProjectState }`
- [x] `shared/src/services/action/` — ActionService（アクション名+ハンドラ登録、`keydown` 一元管理、テキスト入力中J/K/Space無効化）
- [x] `shared/src/services/dialog/` — DialogService（`shallowRef` スタック、DOM順で重なり解決）
- [x] `shared/src/services/undo-redo/` — UndoRedoService（コマンドパターン、undo/redo 関数ペアの履歴）
- [x] 各サービスにインターフェースを定義
- [x] **確認**: `pnpm typecheck` 通過

### ステップ 5: 永続化サービス（Dexie.js / IndexedDB）

- [x] `shared/src/services/persistence/database.ts` — Dexie スキーマ（settings テーブル、project テーブル）
- [x] `shared/src/services/persistence/persistence-service.ts` — 保存/復元 + アプリバージョン埋め込み
- [x] `shared/src/services/persistence/migration.ts` — マイグレーション関数の枠（中身は書かない）
- [x] `shared/src/services/persistence/mock.ts` — MockPersistenceService
- [x] **確認**: `pnpm typecheck` 通過

### ステップ 6: ダイアログ共通コンポーネント

- [x] `shared/src/components/dialog/AppDialog.vue` — @headlessui/vue Dialog ベース（slot: body, footer）
- [x] `shared/src/components/dialog/ConfirmDialog.vue` — 確認ダイアログ
- [x] `shared/src/components/dialog/DialogHost.vue` — DialogService スタックを描画（App.vue 直下）
- [x] **確認**: DialogService 経由で確認ダイアログ表示・複数同時表示・個別クローズ動作

### ステップ 7: トースト通知コンポーネント

- [x] `shared/src/services/toast/` — ToastService シングルトン
- [x] `shared/src/components/toast/ToastHost.vue`
- [x] **確認**: `toastService.show(...)` でトースト表示・数秒で消える

### ステップ 8: UI スケルトン（レイアウト枠）

- [x] `shared/src/App.vue` — 全体レイアウト（DialogHost, ToastHost 含む）
- [x] `shared/src/components/layout/AppToolbar.vue` — ツールバー枠
- [x] `shared/src/components/layout/SampleListPane.vue` — 左ペイン枠
- [x] `shared/src/components/layout/EditorPane.vue` — 右ペイン枠
- [x] `shared/src/components/layout/PlaybackControlArea.vue` — 再生コントロール枠
- [x] `shared/src/components/layout/AccentEditorArea.vue` — アクセント編集エリア枠
- [x] `shared/src/components/ErrorBoundary.vue` — エラーバウンダリー
- [x] **確認**: 各エリアに仮テキストが表示される、Tailwind でレイアウトが組まれている

### ステップ 9: .lab パーサーとアクセントデータパーサー

- [x] `shared/src/domain/lab-parser.ts` — Julius 形式 .lab パーサー
- [x] `shared/src/domain/phoneme-to-mora.ts` — 音素列→モーラ列変換（split_mora 相当）
- [x] `shared/src/domain/ojt-to-hiragana.ts` — OJT 音素→ひらがな変換テーブル
- [x] `shared/src/domain/accent-parser.ts` — アクセント 4 ファイル(0/1配列)パース、モーラ長変換
- [x] `shared/src/domain/accent-logic.ts` — 高低パターン計算、分割・結合時のアクセント初期値
- [x] **確認**: モックデータに対してパーサーが正しく動作

### ステップ 10: プロジェクトデータ構築（モックデータからの読み込み）

- [x] `shared/src/domain/project-builder.ts` — FsAdapter + パーサーで ProjectData 構築
- [x] `shared/src/domain/natural-sort.ts` — 自然順ソート
- [x] AppStateService に project セットメソッド追加
- [x] **確認**: `?mock=all` でモックからProjectData構築、`phase: "editing"` になる

### ステップ 11: 左ペイン（サンプル一覧）

- [x] `SampleListPane.vue` 完全実装 — stem 一覧、選択、確認済みマーク、検索、フィルタ
- [x] J/K キーでのサンプル移動（ActionService 経由）
- [x] サンプル移動時に前のサンプルを確認済みにする
- [x] Esc キーで検索クリア+フォーカス解除
- [x] **確認**: 一覧表示、クリック選択、検索/フィルタ、J/K 移動、確認済みフラグ動作

### ステップ 12: アクセント編集エリア（モーラ表示 + 高低折れ線 + 境界表示）

- [x] `shared/src/components/editor/AccentEditor.vue` — エディタ全体
- [x] `shared/src/components/editor/MoraRow.vue` — モーラ文字列行（ひらがな）
- [x] `shared/src/components/editor/PitchLine.vue` — 高低折れ線行（SVG、句ごと独立）
- [x] `shared/src/components/editor/BoundaryToggle.vue` — 境界トグルボタン + 縦線
- [x] まず折り返しモードのみ実装
- [x] **確認**: モーラがひらがな表示、句ごとの折れ線描画、境界の縦線とトグルボタン表示

### ステップ 13: アクセント位置ハンドル + 編集操作 + Undo/Redo

- [x] `shared/src/components/editor/AccentHandle.vue` — ドラッグ/クリックでスナップ移動
- [x] `shared/src/domain/accent-edit-commands.ts` — 境界トグルコマンド、アクセント移動コマンド
- [x] UndoRedoService との接続
- [x] **確認**: ハンドル操作、境界トグル、Ctrl+Z/Y で Undo/Redo、折れ線即時更新

### ステップ 14: 音声再生サービスとモック

- [x] `shared/src/services/audio/interface.ts` — 再生インターフェース
- [x] `shared/src/services/audio/audio-service.ts` — Web Audio API ベース
- [x] `shared/src/services/audio/mock.ts` — MockAudioService（無音で時間経過）
- [x] **確認**: モックで再生/停止動作確認

### ステップ 15: 再生コントロール UI + 再生ハイライト + 自動スクロール

- [x] `PlaybackControlArea.vue` 完全実装 — 再生/停止ボタン、速度プリセット
- [x] モーラ/句クリック時の自動再生、スペースキー再生
- [x] 再生中ハイライト（requestAnimationFrame）
- [x] 自動スクロールモード（しない / 画面外で追従 / 常に中心）
- [x] 音声未設定時は再生UI全体を disabled 表示（仕様書 7.2）
- [x] Esc キー（検索欄以外）でモーラ/句選択を解除＋再生停止（仕様書 14）
- [x] **確認**: Space で再生/停止、モーラクリックで自動再生、ハイライト動作、音声未設定時の disabled 状態

### ステップ 16: プロジェクト保存/読み込み + dirty 判定

- [ ] `shared/src/domain/project-save-load.ts` — プロジェクト JSON 保存/読み込み + 読み込み時 stems 検証
- [ ] `shared/src/domain/dirty-check.ts` — dirty 判定ロジック（最終保存時点との全フィールド差分比較）
- [ ] dirty 時の読み込み確認ダイアログ
- [ ] Ctrl/Cmd+S、Ctrl/Cmd+O ショートカット（ActionService 経由）
- [ ] **確認**: 保存でJSONダウンロード、読み込みで復元、dirty 状態表示、dirty 時の確認ダイアログ

### ステップ 17: ツールバー + 設定ダイアログ

- [ ] `AppToolbar.vue` 完全実装 — 保存/読み込み/一括書き出し/設定ボタン配置
- [ ] `shared/src/components/dialog/SettingsDialog.vue` — グローバル設定ダイアログ（表示モード、自動スクロール、再生速度）
- [ ] **確認**: ツールバーの各ボタンが機能する、設定変更が反映・自動保存される

### ステップ 18: 書き出し（個別/一括）

- [ ] `shared/src/domain/export.ts` — 個別/一括書き出しロジック（ブラウザ版 ZIP は JSZip を依存追加）
- [ ] **確認**: 個別書き出しでJSONダウンロード、一括書き出しでZIPダウンロード、成功トースト表示

### ステップ 19: 新規プロジェクト作成フロー

- [ ] `shared/src/components/dialog/NewProjectDialog.vue` — glob 入力、マッチ件数表示、D&D 対応、進捗カウンタ
- [ ] glob テンプレートを IndexedDB に保存（開始ボタン押下時のみ更新。入力中はリアルタイム保存しない）
- [ ] `shared/src/components/welcome/WelcomeScreen.vue` — 初期画面（新規作成 or 読み込み or 自動復元）
- [ ] **確認**: `?mock=all` でモックからプロジェクト作成→編集画面遷移

### ステップ 20: BrowserFsAdapter 実装

- [ ] `shared/src/services/fs-adapter/browser.ts` — File System Access API ベース
- [ ] `packages/web/src/main.ts` で BrowserFsAdapter 登録
- [ ] **確認**: ブラウザ版で実ファイルでのプロジェクト作成・保存・読み込み動作

### ステップ 21: ElectronFsAdapter 実装 + IPC

- [ ] `shared/src/services/fs-adapter/electron.ts` — IPC 経由、パス区切り変換（`/` ↔ OS ネイティブ）
- [ ] `packages/electron/electron/main.ts` — IPC ハンドラ追加
- [ ] `packages/electron/electron/preload.ts` — contextBridge 経由で最小ファイル I/O API のみ公開（現状の ipcRenderer 丸公開を書き換え）
- [ ] **確認**: Electron 版で実ファイルでのプロジェクト作成・保存・読み込み動作

### ステップ 22: 横スクロールモード + 表示モード切り替え

- [ ] `AccentEditor.vue` 修正 — 表示モード切り替え対応
- [ ] `shared/src/components/editor/ScrollContainer.vue` — 横スクロール同期コンテナ
- [ ] **確認**: 折り返し/横スクロール切り替え、横スクロール時の全行同期

### ステップ 23: 音声 glob 後から設定 + 自動保存連携

- [ ] `shared/src/components/dialog/AudioGlobDialog.vue` — 音声 glob 設定ダイアログ（紐付け不可の stem 数をトースト通知）
- [ ] 変更時の IndexedDB 自動保存トリガー
- [ ] **確認**: 音声 glob 後設定、自動保存動作

### ステップ 24: 最終仕上げ

- [ ] 全ショートカットキー最終確認（仕様書 14 の一覧）
- [ ] Ctrl+U で現在サンプルを未確認に戻す
- [ ] アプリ起動時の IndexedDB 自動復元
- [ ] `pnpm typecheck`, `pnpm format`, `pnpm lint` 全通過
- [ ] **確認**: 仕様書の全機能が動作する

---

## 設計上の重要ポイント

### サービス初期化順序（各ターゲットの main.ts）

1. MockConfig（URLパラメータ解析）
2. FsAdapter（モック or 実体を選択・注入）
3. AudioService（モック or 実体を選択・注入）
4. DialogService / ToastService（Vue reactive のみ使用、mount 前に生成可能）
5. PersistenceService（DBオープンのみ。マイグレーション確認はまだ行わない）
6. ActionService（keydown リスナー登録）
7. Vue app mount
8. AppStateService 初期化（PersistenceService からの復元 + マイグレーション失敗時は DialogService でダイアログ表示）

### サービス注入パターン

- 各サービスファイルから export されたシングルトンインスタンスを直接 import
- FsAdapter と AudioService は、ターゲットで実装が異なるため `setFsAdapter(adapter)` / `setAudioService(service)` でランタイム注入

### CLAUDE.md 制約への対応

- `!` → `== null` チェック or optional chaining
- `as` → zod バリデーション結果の型をそのまま使う
- falsy/truthy チェック → 明示的な比較（数値: `=== 0`、文字列: `=== ""`）
- null/undefined チェック → `== null`（`===` は使わない。null と undefined を同時にチェック）
- 引数デフォルト値 → 全引数必須
- UnreachableError → switch-case の default で throw

## 検証方法

1. 各ステップの完了条件で記載の確認を行う
2. 最終的に `pnpm typecheck`, `pnpm format`, `pnpm lint` を実行して全通過を確認
3. `?mock=all` でモック動作確認、実ファイルでの動作確認を両方行う
