# 日本語アクセント位置可視化・修正ツール 仕様書

---

## 1. 概要

音声合成用の日本語アクセントデータを可視化し、実際の音声と聞き比べながらアクセント句境界およびアクセント位置を修正するツール。

修正結果はサンプルごとの JSON ファイルとして書き出せる。

---

## 2. 実行環境・技術スタック

### 2.1 対応プラットフォーム

| プラットフォーム | 説明                                           |
| ---------------- | ---------------------------------------------- |
| ブラウザ版       | 通常の Web ブラウザ上で動作（Electron 不使用） |
| Electron 版      | デスクトップアプリとして動作                   |

両バージョンを最初から並行実装する。

### 2.2 フロントエンド

- **フレームワーク**: Vue 3（Composition API）
- **ビルドツール**: Vite
- **言語**: TypeScript（`strict: true`）
- **スタイル**: Tailwind CSS
- **UI コンポーネント**: @headlessui/vue
- **アイコン**: @heroicons/vue
- **状態管理**: Vue の `reactive` / `ref` のみ（Pinia・Vuex は使用しない）
- **ルーティング**: Vue Router は使用しない（コンポーネント切替のみ）

### 2.3 永続化

- **ライブラリ**: Dexie.js（IndexedDB）
- ブラウザ版・Electron 版ともにレンダラー側の IndexedDB に自動保存する

### 2.4 Electron 固有

- **パッケージャー**: electron-builder
- **アーキテクチャ**: `contextIsolation: true`、`contextBridge` 経由で最小 API のみ公開
- ファイルシステムアクセス（ダイアログ・読み書き）はメインプロセスに寄せ、レンダラーとは IPC で通信

### 2.5 パッケージ管理・モノレポ

- **パッケージマネージャー**: pnpm workspace
- UI（Vue アプリ本体）は共有パッケージとして配置し、web / electron の両ターゲットから参照する
- ブラウザ版とElectron版でファイル入出力の実装を差し替えられるよう、抽象インタフェース（`FsAdapter` 等）を定義し、`BrowserAdapter` / `ElectronAdapter` で実装する

### 2.6 コード品質

- **リンター**: ESLint（@vue/eslint-config-typescript + eslint-plugin-vue、Flat Config）
- **フォーマッター**: Prettier（デフォルト設定）
- ローカルでの `pnpm lint` および `pnpm format:check` を必須運用とする
- テスト・CI・コミットフックはプロトタイプ段階では実装しない
- 将来的なテストツール: Vitest（単体テスト）、Playwright（E2E）

### 2.7 パス表記

プロジェクト JSON 内のパスはすべて `/` 区切りで統一する。  
Electron 版は読み書き時に OS のパス区切りと変換する。

---

## 3. 入力データ

### 3.1 ファイル種別

各サンプルは以下の最大 6 種類のファイルから構成される。

| 種別                       | 内容                                            | 必須/任意 |
| -------------------------- | ----------------------------------------------- | --------- |
| `.lab` ファイル            | Julius 形式の音素アライメント（OJT 音素セット） | **必須**  |
| `start_accent_list`        | アクセントの開始位置（音素長の 0/1 配列）       | **必須**  |
| `end_accent_list`          | アクセントの終了位置（音素長の 0/1 配列）       | **必須**  |
| `start_accent_phrase_list` | アクセント句の開始位置（音素長の 0/1 配列）     | **必須**  |
| `end_accent_phrase_list`   | アクセント句の終了位置（音素長の 0/1 配列）     | **必須**  |
| 音声ファイル（wav 等）     | 再生用音声                                      | **任意**  |

音声ファイルが未設定の場合は再生機能のみ無効となり、編集・書き出しは通常通り行える。

### 3.2 サンプル紐付けルール

- 各ファイル種別ごとに glob パターンを指定する
- サンプルの識別キー（stem）は **ファイル名から拡張子を除いたもの** とする
- 各 glob のマッチ結果を stem ごとに紐付ける

### 3.3 アクセントデータのモーラ変換

入力のアクセント 4 ファイルは音素長の配列だが、ツール内部ではモーラ長に変換して扱う。  
変換は `.lab` から `split_mora`（母音インデックス抽出）相当の処理で行う。

---

## 4. 新規プロジェクト作成

### 4.1 操作フロー

1. ルートディレクトリを選択する（ブラウザ版: `<input webkitdirectory>` またはドラッグ&ドロップ、Electron 版: フォルダ選択ダイアログまたはドラッグ&ドロップ）
2. 5 種の必須 glob と 1 種の任意音声 glob を入力する
3. 各 glob 入力欄からフォーカスが外れるたびにマッチ件数を自動計算・表示する
4. 以下の 2 つの件数を表示する：
   - 各 glob のマッチファイル数
   - 5 種の stem 集合の共通部分サイズ（候補サンプル数）
5. 必須 5 種 glob のいずれかのマッチ件数が 0 の場合、開始ボタンを無効化する
6. 開始ボタン押下時に glob パターンをグローバルテンプレートとして保存し、走査・検証を開始する
7. 進捗カウンタを表示する（候補サンプル数に対する処理数）
8. 完了後、自動的に編集画面へ遷移し、先頭 stem を開く

### 4.2 glob テンプレート

- グローバルテンプレートは IndexedDB に保存され、次回の新規作成画面でデフォルト値として読み込まれる
- 開始ボタン押下時に更新される（入力中はリアルタイム保存しない）

---

## 5. プロジェクトファイル

### 5.1 ファイル仕様

- 拡張子: `.accentproj.json`
- デフォルトファイル名: `project.accentproj.json`

### 5.2 JSON スキーマ

```
{
  "version": 1,
  "meta": {
    "root_directory": "<ルートディレクトリパス（ブラウザ版は空可）>",
    "glob_lab": "<glob パターン>",
    "glob_start_accent": "<glob パターン>",
    "glob_end_accent": "<glob パターン>",
    "glob_start_accent_phrase": "<glob パターン>",
    "glob_end_accent_phrase": "<glob パターン>",
    "glob_audio": "<glob パターン（任意）>"
  },
  "stems": ["<stem1>", "<stem2>", ...],
  "samples": {
    "<stem>": {
      "moras": ["か", "な", ...],
      "mora_intervals": [{"start": 0.0, "end": 0.1}, ...],
      "phrase_boundaries": [<モーラインデックス>, ...],
      "accent_pos_in_phrase": [<句内0始まり位置>, ...],
      "source_files": {
        "lab": "<相対パス>",
        "start_accent": "<相対パス>",
        "end_accent": "<相対パス>",
        "start_accent_phrase": "<相対パス>",
        "end_accent_phrase": "<相対パス>"
      }
    }
  },
  "overrides": {
    "<stem>": {
      "phrase_boundaries": [...],
      "accent_pos_in_phrase": [...]
    }
  },
  "checked": {
    "<stem>": true
  },
  "audio_files": {
    "<stem>": "<相対パス>"
  },
  "last_open_stem": "<stem>"
}
```

- `meta` 内の情報は表示用・参照用のメタ情報であり、プロジェクト再開時には使用しない
- プロジェクト作成後、`.lab` とアクセント 4 ファイルは参照しない。編集の正は `samples` 内のデータのみとする
- `mora_intervals` の値は `.lab` から読んだ値をそのまま保存する（丸め処理なし）
- `stems` の並び順は自然順ソート（数字部分は数値比較、大小文字区別なし）

### 5.3 自動保存

プロジェクトの状態は IndexedDB に自動保存される（プロジェクト JSON と同等の内容）。

### 5.4 dirty 判定

最後の保存時点からプロジェクト JSON の全フィールド（`last_open_stem` を含む）に差分があれば dirty と判定する。

---

## 6. プロジェクトの保存・読み込み

### 6.1 保存

| バージョン  | 操作                                        |
| ----------- | ------------------------------------------- |
| ブラウザ版  | ダウンロード（常に名前付き保存）            |
| Electron 版 | 上書き保存 / 名前を付けて保存（ダイアログ） |

ショートカット: Ctrl/Cmd+S（Electron 版は未保存なら名前付き保存）

### 6.2 読み込み

- ショートカット: Ctrl/Cmd+O
- 現在のプロジェクトが dirty の場合、保存するか確認ダイアログを表示する
- 保存済みの場合は確認なしで置き換える
- 読み込み時、プロジェクト JSON の `stems` と走査結果が一致しない場合はエラーを表示して停止する

### 6.3 アプリ起動時の自動復元

IndexedDB に自動保存されたプロジェクトが存在する場合、起動時に自動的に復元する。  
復元後は `last_open_stem` を開く。`last_open_stem` が見つからない場合は先頭 stem を開く。

### 6.4 アプリ終了時

終了確認・beforeunload 警告は出さない（IndexedDB への自動保存に委ねる）。

---

## 7. 音声設定

### 7.1 音声 glob の後から設定

プロジェクト作成後に音声 glob を設定・変更できる。  
変更時、glob でマッチした音声ファイルを stem で紐付け、一致したものだけ部分的に有効化する。  
紐付けできなかった stem 数を「音声なし: N 件」としてトースト通知する。

### 7.2 音声未設定時の挙動

再生 UI（再生ボタン・速度選択・自動スクロールなど再生関連要素）を disabled 表示にする。  
エラーメッセージは表示しない。

---

## 8. データ定義

### 8.1 モーラ列

- `.lab` の音素列から `split_mora` 相当の処理（母音インデックス抽出）でモーラ列を生成する
- 各モーラは OJT 音素列からひらがなに自動変換して表示する

**特殊モーラの表記**

| 音素                          | 表示                                                          |
| ----------------------------- | ------------------------------------------------------------- |
| pau                           | 「、」（読点）                                                |
| cl                            | 「っ」（促音）                                                |
| A / I / U / E / O（無声母音） | 対応する有声母音（a / i / u / e / o）と同じひらがなとして扱う |

- 「きゃ・きゅ・きょ」等の拗音は 2 文字でも 1 モーラとして扱う
- 「っ」「ん」「ー」も 1 モーラとして扱う

### 8.2 アクセント句境界

- `phrase_boundaries`: アクセント句境界が存在するモーラのインデックス（0 始まり）の配列（昇順・重複なし）
- 「N モーラ目の後ろに境界がある」という意味で N を記録する
- 値の範囲: 0 〜 モーラ数 - 2

### 8.3 アクセント位置

- `accent_pos_in_phrase`: 句ごとの句内 0 始まりアクセント位置の配列
- 各アクセント句に必ず 1 か所だけアクセントが存在する（関東弁規則）
- 範囲: 0 〜 句内モーラ数 - 1

### 8.4 高低パターン（ピッチ表示ルール）

| アクセント位置 | 高低パターン                                          |
| -------------- | ----------------------------------------------------- |
| 0（平板型）    | 1 モーラ目のみ H、2 モーラ目以降 L                    |
| N > 0          | 1 モーラ目 L、2 〜 N+1 モーラ目 H、N+2 モーラ目以降 L |

### 8.5 句の分割・結合時のアクセント初期値

- **分割**: 分割位置にすでにアクセントがある側はそのまま維持。アクセントが存在しない側は 1 モーラ目（index=0）をアクセント位置とする
- **結合**: 前のアクセント句のアクセント位置を採用する

---

## 9. 書き出し（エクスポート）

### 9.1 個別書き出し

- 対象: 現在開いているサンプル
- ファイル名: `<stem>.json`（提案名。ユーザーが変更可能）
- 編集有無にかかわらずその時点の値を出力する
- ブラウザ版: ダウンロード。Electron 版: 保存ダイアログ
- 完了後、成功トーストを表示する（表示時間は未定）

### 9.2 一括書き出し

- 対象: 全 stem（確認済み・未確認にかかわらず）
- 各 stem の `<stem>.json` をフラットに出力する（サブディレクトリ再現なし）
- 出力先/ZIP 名: `accent-jsons`
- ブラウザ版: ZIP としてダウンロード。Electron 版: 出力ディレクトリに保存
- 完了後、成功トーストを表示する

### 9.3 書き出し JSON フォーマット

```json
{
  "phrase_boundaries": [<モーラインデックス>, ...],
  "accent_pos_in_phrase": [<句内0始まり位置>, ...]
}
```

---

## 10. UI レイアウト

### 10.1 全体構成

```
┌─────────────────────────────────────────────┐
│  ツールバー（ヘッダー）                        │
├──────────────┬──────────────────────────────┤
│              │  再生コントロール + 個別書き出し │
│  左ペイン     ├──────────────────────────────┤
│  （一覧）    │  アクセント編集エリア            │
│              │                               │
└──────────────┴──────────────────────────────┘
```

### 10.2 ツールバー

グローバル操作を配置する。

- 保存（Ctrl/Cmd+S）
- 読み込む（Ctrl/Cmd+O）
- 一括書き出し
- グローバル設定（歯車アイコン → モーダル）

### 10.3 左ペイン（サンプル一覧）

- stem 名の一覧を自然順ソートで表示する
- 各行に確認済みチェックマークを表示する
- 上部に検索欄（stem の大小文字区別なし部分一致フィルタ）を配置する
- 「全件表示」／「未確認のみ」の 2 択フィルタを配置する
- Esc キー: 検索欄フォーカス中はテキストクリア＋フォーカス解除

### 10.4 右ペイン

上部に再生コントロールと個別書き出しボタンを配置する。

**再生コントロール**

- 再生/停止ボタン
- 再生速度プリセット: 0.25x / 0.5x / 1x / 2x / 4x（ボタン選択）

**アクセント編集エリア**

モーラ列が横に長い場合、以下の 2 表示モードを切り替えられる（グローバル設定）。

- 折り返しモード: 複数行に折り返す（ハンドル行・折れ線行・モーラ行を同じ単位で折り返す）
- 横スクロールモード: 1 行で横スクロール（全行が同一スクロールコンテナで同期）

編集エリア内の縦順:

1. **アクセント位置ハンドル行**（句ごと）
2. **高低折れ線行**（句ごと、2 値 H/L の折れ線、句ごとに独立したポリライン）
3. **モーラ文字列行**（ひらがな）＋ **境界トグル**（モーラ間）

---

## 11. アクセント編集操作

### 11.1 アクセント句境界トグル

- モーラとモーラの間に小ボタンとして常時表示する
- クリックで ON/OFF をトグルする
- **OFF 状態**: クリック判定はモーラ行付近の狭い範囲
- **ON 状態**: クリック判定をアクセント位置ハンドル行・折れ線行まで縦に拡張する
- 境界位置に縦線を表示する
- 1 クリック = Undo 履歴に 1 ステップ積む

### 11.2 アクセント位置ハンドル

- 各アクセント句の範囲に横長トラックとして常時表示する
- ハンドルをドラッグ、またはトラック上をクリックして句内モーラ位置（0 始まり）にスナップ移動する
- ドラッグ終了時に 1 回だけ Undo 履歴に積む
- 移動すると高低折れ線が即時反映される

### 11.3 高低折れ線

- 各句のアクセント位置に従って H/L の 2 値折れ線を表示する（H=上段、L=下段）
- 句ごとに独立した折れ線（句間で分断）
- アクセント位置変更時に即時更新される

### 11.4 Undo / Redo

- 対象: アクセント句境界トグルとアクセント位置ハンドル移動のみ
- 確認状態の変更は Undo/Redo の対象外
- ショートカット: Ctrl/Cmd+Z（Undo）、Ctrl/Cmd+Y（Redo）

---

## 12. 選択と再生

### 12.1 選択操作

| 操作                     | 選択結果                                       |
| ------------------------ | ---------------------------------------------- |
| モーラ文字をクリック     | モーラを選択（同じモーラを再クリックで解除）   |
| 折れ線行の背景をクリック | アクセント句を選択（同じ句を再クリックで解除） |

モーラ選択と句選択は独立した操作で干渉しない。

### 12.2 自動再生

音声が設定されている場合に有効。

| 操作                       | 自動再生範囲                         |
| -------------------------- | ------------------------------------ |
| モーラクリック（新規選択） | 選択モーラ ± 前後 2 モーラの区間     |
| 折れ線行クリック（句選択） | 句の先頭モーラ開始 〜 句末モーラ終了 |

- 再生中に別のモーラ/句をクリック: 現在再生を停止して新しい区間を再生する
- 再生中に同じモーラ/句を再クリック: 再生を停止する
- 境界トグルおよびハンドル操作後の自動再生はなし

### 12.3 再生区間のルール

- モーラ周辺再生: 文頭・文末でモーラが不足する場合は存在する範囲のみ再生する
- 全区間: 最初のモーラ開始 〜 最後のモーラ終了
- 各区間ともパディングなし

### 12.4 スペースキー

現在の選択状態に従って再生/停止をトグルする。

| 選択状態     | スペースキーの再生対象      |
| ------------ | --------------------------- |
| モーラ選択中 | モーラ周辺（前後 2 モーラ） |
| 句選択中     | 選択句                      |
| 未選択       | 全体                        |

### 12.5 再生中のハイライト

再生中は現在時刻が属するモーラ 1 つをハイライト表示する。更新は `requestAnimationFrame` で行う。

### 12.6 自動スクロールモード（グローバル設定）

| モード       | 動作                                                         |
| ------------ | ------------------------------------------------------------ |
| しない       | スクロールなし                                               |
| 画面外で追従 | ハイライトモーラが画面外に出たらスクロール                   |
| 常に中心     | ハイライトモーラを常に中央に保つ（手動スクロールよりも優先） |

### 12.7 再生速度

- プリセット: 0.25x / 0.5x / 1x / 2x / 4x（グローバル設定として保存）
- 変更は再生中に即時反映する

---

## 13. 確認済みフラグ

- サンプルを開いた状態で別サンプルへ移動（J/K キーまたは一覧クリック）した時点で確認済みになる
- 開いたままアプリを終了した場合は未確認のまま
- Ctrl/Cmd+U: 現在開いているサンプルを未確認に戻す
- 手動での確認済み操作はなし（自動のみ）
- 確認状態の変更は Undo/Redo の対象外

---

## 14. キーボードショートカット

| キー                      | 動作                             |
| ------------------------- | -------------------------------- |
| Space                     | 再生/停止（選択状態に依存）      |
| J                         | 次のサンプルへ移動               |
| K                         | 前のサンプルへ移動               |
| Ctrl/Cmd+Z                | Undo                             |
| Ctrl/Cmd+Y                | Redo                             |
| Ctrl/Cmd+U                | 現在のサンプルを未確認に戻す     |
| Ctrl/Cmd+S                | プロジェクトを保存               |
| Ctrl/Cmd+O                | プロジェクトを読み込む           |
| Esc（検索欄フォーカス中） | 検索文字列クリア＋フォーカス解除 |
| Esc（検索欄以外）         | モーラ/句選択を解除＋再生停止    |

テキスト入力中（検索欄フォーカス中）は Space・J・K を無効にする。

### 14.1 サンプル移動時の動作

J/K 移動は自然順ソート順で前後に移動する。  
移動時は再生を停止し、選択状態をリセットする（未選択状態で新サンプルを開く）。

---

## 15. グローバル設定

グローバル設定は IndexedDB に保存する。設定モーダルは歯車アイコンから開く。

| 設定項目                  | 値                               | 保存場所                  |
| ------------------------- | -------------------------------- | ------------------------- |
| 表示モード                | 折り返し / 横スクロール          | IndexedDB                 |
| 自動スクロールモード      | しない / 画面外で追従 / 常に中心 | IndexedDB                 |
| 再生速度                  | 0.25x / 0.5x / 1x / 2x / 4x      | IndexedDB                 |
| glob テンプレート（5 種） | glob 文字列                      | IndexedDB（開始時に更新） |

---

## 16. 書き出しデータの JSON 形式（編集結果）

各サンプルの書き出し JSON フォーマット（A 形式）:

```json
{
  "phrase_boundaries": [<境界後モーラインデックス>, ...],
  "accent_pos_in_phrase": [<句内0始まりアクセント位置>, ...]
}
```

- `phrase_boundaries`: 昇順・重複なし
- `accent_pos_in_phrase`: 句数と同じ長さの配列

---

_以上_
